{
    "compilerOptions": {
        "module": "ESNext", // Browser-compatible module system
        "target": "ES2022",
        "outDir": "out/blockEditor", // Output directory for compiled files
        "lib": [
            "ES2022",
            "DOM" // Include DOM types for client-side code
        ],
        "sourceMap": true,
        "rootDir": "blockEditor",
        "strict": true,
        "esModuleInterop": true,
        "skipLibCheck": true,
        "baseUrl": ".", // Add baseUrl for path mapping
        "paths": {
            "shared/*": ["shared/*"] // Map "shared" to the shared directory
        }
    },
    "include": [
        "blockEditor/**/*.ts" // Only client-side code
    ],
    "exclude": [
        "node_modules"
    ]
}